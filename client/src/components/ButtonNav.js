import React from 'react'
import { API_URL } from '../constants.js'
import './Button.css';

// shape: Rounded/Icon 'circular'
// size: Small/Medium/Large
// page: Webpage to nav to
// method?: New window/[same window]/refresh
const ButtonNav = ({ shape, size, page, method }) => {
    return (
        <></>
    )
}

export default ButtonNav;