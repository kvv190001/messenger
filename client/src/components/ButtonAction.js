import React from 'react'
import { API_URL } from '../constants.js'
import './Button.css';

// shape: Rounded/Icon 'circular'
// size: Small/Medium/Large
// action: Input/Toggle/CRUD
// context: (Chat/Send) / (Focus/Animate/State) / (HTTPRequest/DOM changes/Variable changes)
// object?: Element object
const ButtonAction = ({ shape, size, action, context, object = null }) => {
    return (
        <></>
    )
}

export default ButtonAction;